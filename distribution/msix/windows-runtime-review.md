# Current Microsoft runtime origin observation

The actual stage from run 34697625639 contains eight MSVC release DLLs and `d3dcompiler_47.dll` that were not bound to an original input by the consumed Muse/Qt-prefix collector. The source collector keeps final source/license closure false until these and publication checks are integrated.

The original Qt-cache `d3dcompiler_47-x64.7z` is 1,380,117 bytes, SHA-256 `b1c379f739fa59a495a388c0878757e3d90fb72e0df771237a1ee4a2c1675d15`. Its sole DLL is 4,173,928 bytes, while the staged DLL is 4,741,488 bytes (`a05f99734f7c4822fefc12b367af21fd0976ed6608752fb1e1e80b6ece7ecbbb`). That archive is not asserted to own the different staged file.

The exact preferred Qt Base 6.11.2 source, `src/tools/windeployqt/utils.cpp::findD3dCompiler`, checks `WindowsSdkDir/Redist/D3D/x64` before Qt's own bin directory and PATH. The actual run records SDK 10.0.26100.0 under `C:/Program Files (x86)/Windows Kits/10`. The CMake cache records `MSVC_REDIST_DIR=C:/Program Files/Microsoft Visual Studio/2022/Enterprise/VC/Redist/MSVC/14.44.35112`; the install log records all eight VC runtime installs immediately after application resources.

The new CMake observer records the actual `InstallRequiredSystemLibraries` list, selected release CRT directory, active VC and SDK roots/version and debug-runtime state. It does not change the install list or any runtime byte. The new PowerShell observer requires exactly the eight configured x64 VC143 release paths and the fixed SDK D3D redistributable path. It refuses foreign roots, additional/missing/duplicate/debug paths, alternate architecture, ADS/traversal aliases and other SDK modes. It then compares actual origin/staged sizes and SHA-256 values, observes a Valid Microsoft Authenticode signature and original company/version metadata, and rechecks both files after observation. It records the exact origin, certificate identity and metadata; mismatch errors retain bounded concrete diagnostics.

The existing no-reparse file/hash and exclusive JSON writer functions were extracted unchanged from the installed helper into `file-io.ps1`. Both callers dot-source this parameterless file. This also avoids importing an installer script's parameter variables into the observer's scope. Installed package/process/certificate/profile/normal-close behavior is unchanged.

The CMake fixture proves the configured selection is observed unchanged and debug state is recorded honestly. The PowerShell production boundary fixture covers the exact plan plus 14 foreign/path/mode mutations, actual file equality, four signature-policy mutations, staged replacement, mutation during signature observation and the non-library execution guard. Its signature reader is explicitly a fixture; **actual Windows signatures/origins remain unverified until a fresh native run**. The existing 10 install sequencing and 9 registration cases pass with the shared helpers. The workflow retains the original configured record and new runtime-origin JSON; it does not declare source closure or publish a binary.

Final local verification: all 116 Python distribution tests pass (81.534 seconds), the runtime and installed PowerShell fixtures pass, every distribution PowerShell file parses, and `git diff --check` passes. The existing branding CMake fixture now includes the two production recorder helpers in its isolated fixture tree so its actual Windows metadata branch still executes in full. The original runner used CMake 3.31.6; its [upstream runtime module](https://github.com/Kitware/CMake/blob/v3.31.6/Modules/InstallRequiredSystemLibraries.cmake) preserves the selected CRT directory and same eight runtime names observed in the install log.
