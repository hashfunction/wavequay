# WaveWeft Effect menu pointer repair

Run `34694396308`, public source `773f0e3a0d797a21e25937cdc3757a61fbf84a1b`, failed during the staged consumer workflow. The original evidence remains unchanged at `/private/tmp/waveweft-34694396308-review/WaveWeft-Windows-qualification/build-evidence/gui`. This repair is separate from the installed MSIX integration commit `7b1a016764b8b4d095ce2f0f30cb7ff0a7ac40eb`.

The failure screenshot was inspected first. It genuinely shows the imported Dawn-thread stereo clip, the complete 0–6 second selection, and the open Effect menu. The original PID 6484 survived. The initial file picker, exact native filename readback, import, clip click, Ctrl+A and Effect click completed. No menu navigation or Reverse input was sent. The primary exception was `Choice focus does not have the exact source-defined role`, after the ten-second `FocusedChoice` wait. Owned cleanup completed; the consumer/audio/project/export workflow did not.

The original UIA tree exposes a visible enabled `Special Menu` / `MenuItem` at `[699,445,209,32]` inside `QQuickView` HWND 393616, class `QQuickView`, automation ID `muse::accessibility::AccessibleAppRootObject.MenuView_WindowView_QQuickView`. The retained editor HWND is 262212. The original receipt did not retain the failing global focus role; this report does not invent it.

Pinned Muse source explains the distinction: `StyledMenuLoader.qml` explicitly uses `PopupView.NoActivateFocus` and `PopupView.NoFocus`, with one delayed navigation-focus request. `StyledMenuItem.qml` supplies the exact MenuItem role and adds ` Menu` for submenus. Its hover handler opens the submenu; a click after hover toggles that same open submenu closed. `StyledMenu.qml` forwards the hovered item's real subitems to the submenu loader. These source contracts justify pointer navigation without asserting a keyboard focus transition.

The driver now discovers the unique source-defined popup/item, checks its nearest native Window to avoid counting nested submenu items through both parent and child, and performs one guarded pointer move to Special. It waits read-only, within the existing bound, for the exact owned Reverse item and clicks it once. It never changes popup focus or repeats a click. A separate menu policy retains and verifies the original main PID/HWND, exact popup class/automation ID/title, full native owner chain, main foreground, hit-root/PID, item role/name/identity, visible/enabled state, full desktop bounds and stable geometry. Both snapshots and actual focus facts are retained, including on failure. Partial mouse input still fails and permits only a freshly guarded release.

The independent Python oracle requires the exact Ctrl+A → Effect click → Special hover → Reverse click sequence and independently checks every new menu ownership/geometry snapshot. Existing combo keyboard behavior, all nine consumer observations, independent reversed PCM/project/recipe checks, module closure, normal close and cleanup predicates remain in force. This change contains no product runtime code and supplies no substitute audio or UI evidence.

Local verification uses production helpers on macOS, with recorded UIA values and explicitly synthetic native owner/foreground fixture facts. The new C# boundary passes 35 mutation cases; the independent Python regression first failed for the missing gate, then passed with 36 ownership, geometry, partial/repeated input and sequence mutations. The existing click, keyboard and filename boundaries pass unchanged, including when both PowerShell fixtures execute sequentially in the same host as CI. The full observer compiles for .NET Framework 4.8 with zero warnings/errors. A fresh Windows run is required to establish the native pointer route and every later consumer stage; these fixtures do not claim native completion.

Original unchanged evidence: `consumer-workflow.json` 96,180 bytes / SHA-256 `7adb5c0908001c7232501641b965a966ea28a97bae094899d111ad3f6a5c9124`; `consumer-failure.png` 83,543 bytes / `eb308d243897ae00cc0aeb13e6de7b894c0cbf6f94dd36c2c4eee2c4bcd3a40a`.

The full distribution suite passes 92 tests in 79.069 seconds (`/private/tmp/waveweft-menu-full-tests.log`); the focused audio/oracle suite passes 20 tests. Changed PowerShell files parse and `git diff --check` passes.
